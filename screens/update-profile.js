import React, {Fragment} from 'react';
import {View, StyleSheet, Text} from 'react-native';
import theme from '../ui-kit/theme';

const UpdateProfile = () => {
  return <Text>UpdateProfile</Text>;
};
export default UpdateProfile;
const styles = StyleSheet.create({});
